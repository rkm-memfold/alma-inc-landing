# /etc/nginx/conf.d/alma-ratelimit.conf — http-context rate limit zone
# for the waitlist signup endpoint (10 signups/min per IP, burst 5).
limit_req_zone $binary_remote_addr zone=waitlist:1m rate=10r/m;
